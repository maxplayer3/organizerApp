import { EnumIteratorPipe } from './enum-iterator.pipe';

describe('EnumIteratorPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumIteratorPipe();
    expect(pipe).toBeTruthy();
  });
});
